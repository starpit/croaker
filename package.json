{
  "name": "nick",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "if [ ! -d wrk ]; then git clone -b latency_stacks https://github.com/starpit/wrk.git && cd wrk && make -j; fi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "columnify": "^1.5.4",
    "parse-duration": "^0.1.1",
    "wrk": "git://github.com/starpit/node-wrk.git#latency_stacks"
  }
}
