{
  "name": "nick",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "if [ ! -d wrk ]; then git clone https://github.com/wg/wrk.git && cd wrk && make -j; fi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parse-duration": "^0.1.1",
    "wrk": "^1.1.0"
  }
}
